class GetDirectionsUseCase {
  /**
      Future<Either<Failure, List<RouteEntity>>> getDirections(
      LocationEntity start, LocationEntity end) async {
      final result = await getIt<DirectionRepository>().getDirections(start, end);
      return result;
      }
   */
}
